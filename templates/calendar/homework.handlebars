{{#if exists}}
    <div class="lesson_homework">
        <div class="homework_text">{{text}}</div>
        {{#files}}
            {{#if showable}}
                <img class="homework_file view" alt="{{original}}" title="{{original}}" src="uploads/thumbnails/{{name}}"/>
            {{else}}
                <a href="uploads/files/{{name}}">
                    <img class="homework_file" alt="{{original}}" title="{{original}}" src="assets/images/file.png"/>
                </a>
            {{/if}}
        {{/files}}
    </div>
{{/if}}